import React from "react";
import { HeroContainer, HeroContent } from "./Hero";

function Hero() {
  return (
    <HeroContainer>
      <HeroContent />
    </HeroContainer>
  );
}

export default Hero;
